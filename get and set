using System;

class Person
{
    private int age;
    public int Age
    {
        get { return age; }
        set { age = value; }
    }
}
class prog
{
    static void Main()
    {
        Person P=new Person();
        P.Age = 20;
        Console.WriteLine("age is"+P.Age);
    }
}
